{{!-- <h1>Welcome to {{bioData.name}}'s bio page!</h1>
<img src="/images/{{bioData.avatar}}" alt="bio image">
<h3>About {{bioData.name}}</h3>
<p>{{bioData.bio}}</p> --}}

<section id="single_album">
  <h1>{{albumsData.album_name}} <span>- {{albumsData.album_year}}</span></h1>
  <div id="infobox">
    <img src="/images/{{albumsData.album_img}}" alt="album image">
    <p>{{albumsData.album_desc}}</p>
  </div>
  <iframe src="{{albumsData.album_video}}" frameborder="0"
    allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
</section>